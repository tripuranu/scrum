#show
  %h1= @date.strftime('%b %-d, %Y')
  %table
    %thead
      %tr
        %th E-Mail
        %th Yesterday
        %th Today
        %th Blockage
        %th Comment
    %tbody
      - @dailyforms.each do |dailyform|
        %tr
          %td= dailyform.user.email
          %td= dailyform.yesterday
          %td= dailyform.today
          %td= dailyform.blockage
          %td= link_to 'comment', admin_dailyform_comment_path(dailyform.id)
